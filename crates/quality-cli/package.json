{
  "name": "@quality/cli",
  "version": "0.4.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release --package quality-cli",
    "check": "cargo fmt --check && cargo clippy --all-targets --all-features --package quality-cli -- -D warnings",
    "test": "cargo test --all-targets --all-features --package quality-cli"
  }
}
