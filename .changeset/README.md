# Changesets

Run `pnpm changeset` for every user-visible change. The release workflow turns
pending changesets into a version pull request, keeps the CLI and Action on the
same version, and publishes native GitHub release assets after that pull request
is merged.
