---
"@quality/cli": minor
---

Add a built-in `santi-og` adapter that discovers `@santi020k/og` workspaces, checks generated Open Graph assets without modifying them, and normalizes stale outputs for terminal, JSON, SARIF, GitHub, and baseline reporting.
